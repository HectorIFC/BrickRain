# Roku device used by `npm run deploy` (developer mode must be enabled on the device).
# Copy this file to `.env` and fill in your values. Never commit `.env`.
ROKU_DEV_TARGET=192.168.1.100
ROKU_DEV_PASSWORD=changeme
